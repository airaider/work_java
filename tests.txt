WEB

Front Controller
String action = request.getParameter("action");
main.do?action=boardlist.do
    <form action="main.do">
    <input type="hidden" name="action" value="boardlist.do">


JDBC
db오류를 웹화면에 전달
Controller에서 throws해서 errorpage
요청시마다 값이 없는 오류
Servlet에서 결과 페이지로 값이 전달되지 않는거 (request에 담을시에 forward / not redirect)
404 / 경로 설정 오류
execute query 잘 수행되는지



모델링 교재 각 단원별 요약
select 조건절
함수 쓸줄 알아야한다
join, subquery
JDBC
driveloading, connect생성, 일반statement, preparedstatement, ???? 바꿔ㅏ주는거, sql 순서대로, 
executeQuery
executeUpdate
resultset handling하기 전까지 close하면 안된당
nextcall, 커서로 옮기고, record가 있으면 true
resultString get String, get int, ....
index로도 뽑을수 있고 column으로도 뽑을 수 있다
dml handling특별히 없다

index뭔지, view뭔지
제약조건, pk, fk, unique, notnull, check