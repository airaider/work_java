singletone
1. private static 해당 클래스 속성 선언
2. 생성자를 private
3. static (해당 클래스) 리턴하는 함수

public class mysingle{
    private static mysingle instance;
    private mysingle(){}
    public static getInstance(){
        if(instance == null){
            instance = new mysingle();
        }
    }
}

final override 금지
상속 또한 불가
클래스 내부에서 속성 선언시에는 값을 지정해야한다

interface
public, static, final
public, abstract

Object class
Object 클래스에는 모든 클래스의 공통적인 기능을 정의한다
다형성

String class
new로 선언시 heap에 할당
String s2 = "ABC"; 로 선언시에는 constance pool에 할당
compareTo : 정렬

Auto Boxing
Integer iobject = 300;
Auto UnBoxing
int ip2 = iobject;

Collection
Set{
    중복저장 불가능
    index 없음
}
List{
    중복 저장 가능
    순서대로 저장
    index 0~size()-1
    중간 삽입은 0~size만 가능
    {
        Arraylist{
            중간 삽입 및 삭제 용으로 사용하지는 않는다
        }
        LinkedList{
            이전 데이터에 대한 hash(double linked list)+value+다음 데이터에 대한 hash
            검색 속도가 array보다 느리다
        }
    }
}

객체의 hashcode를 리턴하는 기능
- override할 필요가 없음
- override 해야 하는 경우
1. set에 동일한 내용의 객체를 저장하고 싶지 않을 때

